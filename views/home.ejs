<%- include('partials/header'); -%>
<h1>Home</h1>
<p><%= homeContent %></p>

<% Diary.forEach( function(diaryEntry) {   %>
      <h3> <%=diaryEntry.title%> </h3>
      <p>  <%= (diaryEntry.content).substr(0,500) + "......" %><a href="/Entries/<%=diaryEntry.title%>" >ReadMore</a> </p>
<% }) %>

<%- include('partials/footer'); -%>
